# Pull CUDA 12.1 wheels directly from PyTorch
--extra-index-url https://download.pytorch.org/whl/cu121

# ------------------------
# PyTorch + CUDA 12.1 stack
# ------------------------
torch==2.2.2+cu121
torchvision==0.17.2+cu121
torchaudio==2.2.2+cu121  # optional but keep versions aligned

# ------------------------
# Core scientific / utils
# ------------------------
numpy==1.26.4
pandas>=2.1
tqdm>=4.66
matplotlib>=3.7
scikit-learn>=1.3

# ------------------------
# Logging & monitoring
# ------------------------
tensorboard>=2.16
fire>=0.6.0           # for lr_finder CLI
torch-lr-finder==0.2.1

# ------------------------
# Optional but recommended
# ------------------------
torchinfo>=1.8.0       # for model summaries
pillow>=10.3           # for image loading (used by torchvision)
ipykernel>=6.29        # if using Jupyter/VSCode notebooks

# ------------------------
#(Optional) augmentations if you reuse class style augmentation
albumentations==1.4.7
opencv-python-headless==4.9.0.80
